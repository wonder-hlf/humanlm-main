# Simulated vs Human Macro Alignment

The human reference metrics are generated from real JUSThink trajectories.

Simulation metrics are not available yet. Generate complete simulated team trajectories, then rerun this script with `--sim-input-dir`.

The current Qwen rollout file contains independent next-turn samples; it is not a complete interactive trajectory and therefore must not be used to claim macro behavioral alignment.
